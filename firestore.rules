rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for authenticated users only
    // This is a basic rule. You should refine it for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
